---
// No props needed for now, but we can add later if needed
---

<section class="hero">
  <div class="container wrap">
    <div class="hero-left">
      <span class="eyebrow">№1 в вентиляции</span>
      <h1>Чистый воздух для всех</h1>
      <p class="subtitle">Univent — профессиональная команда и лидер рынка вентиляции в Узбекистане. 1500+ наименований и 15+ точек продаж по стране.</p>
      <div class="cta">
        <a href="#products" class="btn btn-primary">Каталог</a>
        <a href="#contact" class="btn btn-ghost">Связаться</a>
      </div>
      <div class="hero-stats">
        <div class="stat"><strong>1500+</strong><span>Позиций ассортимента</span></div>
        <div class="stat"><strong>15+</strong><span>Точек продаж</span></div>
        <div class="stat"><strong>№1</strong><span>В сфере вентиляции</span></div>
      </div>
    </div>
    <div class="hero-right">
      <div class="art" role="img" aria-label="Визуализация потока воздуха"></div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 88px 0;
  }
  
  .hero .wrap {
    display: grid;
    grid-template-columns: 1.1fr .9fr;
    gap: 40px;
    align-items: center;
  }
  
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    color: var(--brand-red);
    background: #fff;
    border: 1px solid #ffd6d6;
    padding: 6px 10px;
    border-radius: 999px;
  }
  
  .hero h1 {
    font-size: 48px;
    line-height: 1.05;
    margin: 14px 0 12px;
  }
  
  .subtitle {
    font-size: 18px;
    color: #444;
    max-width: 52ch;
  }
  
  .cta {
    display: flex;
    gap: 12px;
    margin-top: 24px;
  }
  
  .btn {
    appearance: none;
    border: 0;
    border-radius: 12px;
    padding: 14px 18px;
    font-weight: 800;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    transition: transform 0.2s ease;
  }
  
  .btn-primary {
    background: var(--brand-red);
    color: #fff;
    box-shadow: 0 8px 18px rgba(225,29,47,.35);
  }
  
  .btn-primary:hover {
    transform: translateY(-1px);
  }
  
  .btn-ghost {
    background: #fff;
    border: 1px solid #e6e6e6;
    color: var(--text);
  }
  
  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-top: 18px;
  }
  
  .stat {
    background: var(--muted);
    border-radius: 14px;
    padding: 16px;
    text-align: center;
  }
  
  .stat strong {
    display: block;
    font-size: 24px;
  }
  
  .art {
    position: relative;
    height: 440px;
    border-radius: 24px;
    background: radial-gradient(1200px 400px at -10% -30%, rgba(225,29,47,.12), transparent 60%),
              conic-gradient(from 210deg at 80% 40%, rgba(225,29,47,.18), transparent 25% 85%, rgba(225,29,47,.18) 0);
    border: 1px solid #eee;
    box-shadow: var(--shadow);
    overflow: hidden;
  }
  
  .art::after {
    content: "";
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(90deg, rgba(0,0,0,.03), rgba(0,0,0,.03) 1px, transparent 1px, transparent 12px);
  }
  
  @media (max-width: 980px) {
    .hero .wrap {
      grid-template-columns: 1fr;
    }
  }
  
  @media (max-width: 680px) {
    .hero h1 {
      font-size: 38px;
    }
    
    .hero-stats {
      grid-template-columns: 1fr;
    }
    
    .art {
      height: 300px;
    }
  }
</style>